# Datos de conexión a tu Postgres local (base "agrofresh", esquema "lab")
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agrofresh
DB_USER=postgres
DB_PASSWORD=

# Orígenes permitidos para CORS (el frontend corriendo con npm run dev)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
