fastapi>=0.115
uvicorn[standard]>=0.34
psycopg2-binary>=2.9
python-dotenv>=1.0
pydantic>=2.10
openpyxl>=3.1
python-multipart>=0.0.20
reportlab>=4.2
requests>=2.32
boto3>=1.35
google-auth>=2.29
google-auth-oauthlib>=1.2
google-api-python-client>=2.126
# Windows no trae la base de zonas horarias que usa zoneinfo -en Linux sale
# del sistema operativo-, y sin ella el backend no arranca.
tzdata>=2024.1; sys_platform == "win32"

# openpyxl la necesita para medir el logo que va en el Excel del detalle
# del GC. Sin ella el Excel sale igual, pero sin logo.
pillow>=10.0
